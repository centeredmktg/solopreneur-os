[build]
builder = "dockerfile"

[deploy]
# No startCommand here on purpose: Railway runs startCommand without a shell, so
# "${PORT:-8000}" would be passed to uvicorn literally. The Dockerfile CMD uses
# `sh -c`, which expands $PORT correctly — let it own startup.
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
